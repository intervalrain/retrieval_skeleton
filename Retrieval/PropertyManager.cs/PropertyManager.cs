using System;
using Retrieval.SDK;

namespace Retrieval.PropertyManager
{
    public static class PropertyManager
    {
        public static void SetProperty(DataTable table)
        {
        }
    }
}